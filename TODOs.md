# work items to do

- [x] refactor: only publish a single unscience mod, not all the individual mods.  keep the csproj for clean code demarcation to ensure each mod has its own vertical code that wont break others and only shares code via deliberate csproj sharing like ksa-abstractions.lib
- [x] completely remove defunct/unused mods: unladen-swallow, steely-eyed-missile-kitten, stampy, space-tape, red-alert, geeforce, mesh-deform, con-man, average-twr
- [x] copy pngs to <UNSCIENCE_DATA>/pngs during import for both graffiti and free-fallin, read from there automatically
- [x] kitten-animations: per-kitten targeting (not just currently controlled)
- [x] garrys-torch: scaling on independent axis
- [x] garrys-torch: when a weldee (source) is welded, animations on that thing (like light part actuation) are not working
- [x] godzilla (new mod): vessel scaling.  "smart" scaling where we dont break part offsets and not smart (what garrys-torch does now)
- [x] zippo: disco light show
- [x] byo-music: can this be tied to in-game location / 3d sound? repeat? "remember me" giant bender.  we should be able to import sound files in supported formats like ogg (not sure if brutal supports wav, mp3 as well) using our existing file/dir picker, and place them into a <UNSCIENCE_DATA>/sounds dir (copy them there like we do for pngs and glbs) and use this folder of files to drive filterable dropdowns to pick sounds.  can we target a particular vessel and have the sound be tied to it in 3d space?  if so can we set it on a loop like a continuous repeat with an option for how long between each repeat?
- [x] pyro: animation cycle options.  an on/off infinite loop with a time for how long off and how long on in seconds, use dragfloat sliders.  this should be a runtime state thing.
- [x] pebbles: i want the imported glbs to get copied to a <UNSCIENCE_DATA>/glbs folder using the same file/folder browser/picker pattern that we do for the PNGs so that anything importing glbs can share this, and any mesh pickers should automatically detect from this folder.  there will be a new mod "sphinx" trying to reuse these after this refactor.
- [x] sphinx: place arbitrary statics from imported GLB models (meshes and textures), auto align with ground, rotation, translation offsets, scaling options. note we have a shared glbs import pattern already from pebbles, reuse that similar to how we reuse the pngs for graffiti and free-fallin. the intent is to place one-off statically positioned (probably) large textured meshes.  most likely the user will import a textured glb and we should support the easy path of just using that mesh+texture, but also note the user may want to change the texture to some other png we have available in the common pngs UNSCIENCE_DATA png dir.  also keep in mind this is just for fun and we want it to mostly "just work" so importing glbs with advanced unsupported features, if we can just ignore unsupported stuff and make it still work like pebbles does now, thats preferable.
- [ ] SKIP FOR NOW: its-so-shiny: completely broken
- [ ] SKIP FOR NOW: blinky: completely broken
- [ ]

- [x] garrys-torch and graffiti - when garrys-torch welded source is stretched out and then a decal applied with graffiti, then the decal is not always fully ontop visually and the depth option doesnt seem to do anything, if i put positive values it disappears entirely and any negative value gets swapped to "auto" and doesnt seem to fix anything 
- [x] zippo: disco is working however everything animates colors and actuation and beam width changes all at the same time, there needs to be a jitter that makes them run at different times
- [x] garrys-torch: due to recent changes to welding behavior so that part animations now work while welded, now sources to welds are participating in collision physics, which they previously did not.  this was not a desired side effect.  is it possible to retain the animation behavior (which now works), but still not participate in collisions for the source of the weld?  if this is possible, ideally we'd make this a boolean value on the weld (default to false) for "collisions", if we can't toggle it so simply but can still disable this behavior, then do this
- [x] sphinx: make the changes to the existing static live changes when edited.  can we also add texture settings to change like texture uv offsets or scaling to make it fit the mesh better?  with user exposed controls to do so?
- [x] sphinx: need to support colliders with some smart auto detection if possible, similar to pebbles
- [x] iron-man (new): make a new mod called "iron-man", is it possible to make modifications to the game such that it will allow KittenEva's to be treated like regular vessels and be able to go into the vehicle editor and attach parts to them?  In the XML you cannot do this in advance with connectors and such to add tanks and engines or RCS blocks, so we would need a runtime solution that modifies the game such that an existing KittenEva can be switched into editor mode and that we can either automatically place a pair of connectors inside pointing up and down (preferable) or expose a way in the editor to manage the connectors on the KittenEva (more preferable if possible).  The goal here would be to make it so we can add some e.g. small fuel tanks and some small engines and fly around like having rocket powered boots. do deep research on the ksa-game-assemblies decompiled sources and look for a smart and targeted way to at runtime enable this to work.  if possible the code changes should be arranged so they are off by default and only enabled explicitly via the new mod interface.  use the ksa, imgui, harmony skills and be an expert orchestrator of subagents to do the research and implement this accurately and effectively and efficiently